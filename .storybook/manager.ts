import { addons } from '@storybook/addons';

addons.setConfig({
  brandTitle: 'Echo UI Kit',
  brandImage: '/storybook-logo.svg', // ⬅️ Put this image in `public/` folder
  showThemeSwitch: true,
});
